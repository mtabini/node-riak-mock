{
  "name": "mock-riak",
  "version": "0.8.6",
  "author": "Marco Tabini <marcot@tabini.ca>",
  "description": "A mock library that simulates a running Riak cluster (without actually having one)",
  "homepage": "https://github.com/mtabini/node-riak-mock",
  "repository": {
    "type": "git",
    "url": "https://github.com/mtabini/node-riak-mock.git"
  },
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha --harmony -R dot -u tdd $(find tests -name 'test_*.js')"
  },
  "dependencies" : {
    "restify"  :  "2.4.x"
  },
  "devDependencies": {
    "mocha"    :  ">=1.9.0",
    "chai" : ">=1.5.0",
    "request" : ">=2.21.0",
    "riak-js" : "*",
    "bunyan": "*",
    "async": "*"
  },
  "engines": {
    "node": ">=0.10"
  }
}

